# OpenTalking Studio Phase 1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a formal `apps/studio` TypeScript application with the approved light mint visual system, authenticated app shell, core route pages, asset and solution-kit domain models, and a working production build.

**Architecture:** Create a new Vite React app under `apps/studio` instead of extending `apps/web`. Keep the Studio app modular: `app/` owns routing and layout, `entities/` owns typed product data, `features/` owns reusable domain widgets, `pages/` owns route composition, and `shared/` owns UI primitives and design tokens.

**Tech Stack:** React 18, TypeScript 5.6, Vite 5, Tailwind CSS 3, `lucide-react`, Node/npm scripts consistent with `apps/homepage` and `apps/web`.

## Global Constraints

- Studio is a new app under `apps/studio`.
- Use React 18, TypeScript, Vite, Tailwind CSS, and `lucide-react`.
- Do not use one-off static HTML for implementation; brainstorming HTML is visual reference only.
- Do not introduce Next.js for the first Studio version.
- Use a light mint visual direction with low-saturation warm action buttons.
- Use `#0F766E` for primary teal, `#D98B5F` for high-priority action, `#F8FAFC` for background, and `#102033` for primary text.
- Use the warm action color only for create, confirm, upgrade, and publish actions.
- Use icons from `lucide-react`; do not use emojis as icons.
- Keep cards at modest radii: controls `8px`, cards `10px`, panels `12px`.
- First authenticated route is `/workspace`.
- Solution kits are first-class product objects.
- Keep `apps/web/src/App.tsx` untouched for Phase 1.

---

## File Structure

Create these files:

- `apps/studio/package.json`: Studio package scripts and dependencies.
- `apps/studio/index.html`: Vite HTML entry.
- `apps/studio/vite.config.ts`: Vite React configuration.
- `apps/studio/tsconfig.json`: TypeScript settings.
- `apps/studio/postcss.config.js`: Tailwind PostCSS pipeline.
- `apps/studio/tailwind.config.js`: Tailwind content and theme extension.
- `apps/studio/src/main.tsx`: React root bootstrap.
- `apps/studio/src/index.css`: Tailwind layers, base styles, focus states.
- `apps/studio/src/vite-env.d.ts`: Vite type reference.
- `apps/studio/src/app/App.tsx`: Top-level route dispatcher.
- `apps/studio/src/app/routes.ts`: Route ids, route metadata, and path helpers.
- `apps/studio/src/app/StudioLayout.tsx`: Authenticated sidebar/topbar layout.
- `apps/studio/src/app/AuthLayout.tsx`: Login/register/trial layout.
- `apps/studio/src/entities/asset.ts`: Asset types, fixtures, and filters.
- `apps/studio/src/entities/solution-kit.ts`: Solution kit types and fixtures.
- `apps/studio/src/entities/project.ts`: Project types and fixtures.
- `apps/studio/src/entities/quota.ts`: Quota type and fixture.
- `apps/studio/src/entities/user.ts`: User type and fixture.
- `apps/studio/src/shared/design-system/theme.ts`: Theme token constants.
- `apps/studio/src/shared/ui/Badge.tsx`: Reusable badge component.
- `apps/studio/src/shared/ui/Button.tsx`: Reusable button component.
- `apps/studio/src/shared/ui/Card.tsx`: Reusable card/panel component.
- `apps/studio/src/shared/ui/MetricCard.tsx`: Compact stat card component.
- `apps/studio/src/features/assets/AssetCard.tsx`: Asset card.
- `apps/studio/src/features/assets/AssetGrid.tsx`: Asset grid.
- `apps/studio/src/features/solution-kits/SolutionKitCard.tsx`: Solution kit card.
- `apps/studio/src/features/projects/ProjectList.tsx`: Recent project list.
- `apps/studio/src/pages/AuthPages.tsx`: Login/register/trial page variants.
- `apps/studio/src/pages/WorkspacePage.tsx`: Main workspace.
- `apps/studio/src/pages/CreateVideoPage.tsx`: Video creation shell.
- `apps/studio/src/pages/CreateRealtimePage.tsx`: Real-time human shell.
- `apps/studio/src/pages/AssetLibraryPage.tsx`: Asset center shell.
- `apps/studio/src/pages/SolutionKitsPage.tsx`: Solution kits listing.
- `apps/studio/src/pages/SolutionKitDetailPage.tsx`: Solution kit detail.
- `apps/studio/src/pages/ProjectsPage.tsx`: Projects listing.
- `apps/studio/src/pages/UtilityPages.tsx`: Billing/team/API/settings simple pages.
- `apps/studio/src/entities/asset.test.ts`: Asset behavior tests.
- `apps/studio/src/app/routes.test.ts`: Route behavior tests.

Modify these files:

- None outside `apps/studio` for Phase 1 unless dependency lockfiles are generated by `npm install`.

---

### Task 1: Scaffold Studio Package And Test Harness

**Files:**
- Create: `apps/studio/package.json`
- Create: `apps/studio/index.html`
- Create: `apps/studio/vite.config.ts`
- Create: `apps/studio/tsconfig.json`
- Create: `apps/studio/postcss.config.js`
- Create: `apps/studio/tailwind.config.js`
- Create: `apps/studio/src/main.tsx`
- Create: `apps/studio/src/index.css`
- Create: `apps/studio/src/vite-env.d.ts`

**Interfaces:**
- Produces npm scripts: `npm run dev`, `npm run build`, `npm run typecheck`, `npm test`.
- Produces app root element id: `root`.

- [ ] **Step 1: Create package and configuration files**

Create `apps/studio/package.json`:

```json
{
  "name": "opentalking-studio",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "preview": "vite preview --host 127.0.0.1"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "lucide-react": "^0.468.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.49",
    "tailwindcss": "^3.4.15",
    "typescript": "~5.6.3",
    "vite": "^5.4.11",
    "vitest": "^2.1.9"
  }
}
```

Create `apps/studio/index.html`:

```html
<!doctype html>
<html lang="zh-CN">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>OpenTalking Studio</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

Create `apps/studio/vite.config.ts`:

```ts
import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";

export default defineConfig({
  plugins: [react()],
});
```

Create `apps/studio/tsconfig.json`:

```json
{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "lib": ["DOM", "DOM.Iterable", "ES2020"],
    "allowJs": false,
    "skipLibCheck": true,
    "esModuleInterop": true,
    "allowSyntheticDefaultImports": true,
    "strict": true,
    "forceConsistentCasingInFileNames": true,
    "module": "ESNext",
    "moduleResolution": "Node",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx",
    "types": ["vitest/globals"]
  },
  "include": ["src"],
  "references": []
}
```

Create `apps/studio/postcss.config.js`:

```js
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
```

Create `apps/studio/tailwind.config.js`:

```js
/** @type {import('tailwindcss').Config} */
export default {
  content: ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"],
  theme: {
    extend: {
      colors: {
        studio: {
          background: "#F8FAFC",
          surface: "#FFFFFF",
          mint: "#F6FBFA",
          sidebar: "#ECFDF5",
          primary: "#0F766E",
          primaryStrong: "#115E59",
          primarySoft: "#CCFBF1",
          action: "#D98B5F",
          actionStrong: "#C8754F",
          actionSoft: "#FFF4ED",
          info: "#2563EB",
          text: "#102033",
          muted: "#64748B",
          border: "#DBE7E8",
          borderSoft: "#EAF2F3",
        },
      },
      boxShadow: {
        studio: "0 18px 42px rgba(15, 23, 42, 0.08)",
      },
    },
  },
  plugins: [],
};
```

Create `apps/studio/src/vite-env.d.ts`:

```ts
/// <reference types="vite/client" />
```

Create `apps/studio/src/index.css`:

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

:root {
  color: #102033;
  background: #f8fafc;
  font-family: Inter, "PingFang SC", "Microsoft YaHei", system-ui, -apple-system, BlinkMacSystemFont, sans-serif;
}

* {
  box-sizing: border-box;
}

body {
  margin: 0;
  min-height: 100vh;
  background:
    radial-gradient(circle at 12% 0%, rgba(204, 251, 241, 0.72), transparent 32%),
    radial-gradient(circle at 88% 5%, rgba(255, 244, 237, 0.72), transparent 30%),
    #f8fafc;
}

button,
a {
  cursor: pointer;
}

:focus-visible {
  outline: 3px solid rgba(15, 118, 110, 0.32);
  outline-offset: 2px;
}
```

Create `apps/studio/src/main.tsx`:

```tsx
import React from "react";
import ReactDOM from "react-dom/client";
import { App } from "./app/App";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root") as HTMLElement).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
);
```

- [ ] **Step 2: Create temporary App so build can run**

Create `apps/studio/src/app/App.tsx`:

```tsx
export function App() {
  return (
    <main className="min-h-screen bg-studio-background p-8 text-studio-text">
      <h1 className="text-2xl font-semibold">OpenTalking Studio</h1>
    </main>
  );
}
```

- [ ] **Step 3: Install Studio dependencies**

Run:

```bash
cd apps/studio
npm install
```

Expected: `node_modules` and `package-lock.json` are created for `apps/studio`.

- [ ] **Step 4: Verify initial build**

Run:

```bash
cd apps/studio
npm run typecheck
npm run build
```

Expected: both commands exit with code 0.

- [ ] **Step 5: Commit**

```bash
git add apps/studio
git commit -m "feat: scaffold studio app"
```

---

### Task 2: Add Route Metadata With TDD

**Files:**
- Create: `apps/studio/src/app/routes.test.ts`
- Create: `apps/studio/src/app/routes.ts`

**Interfaces:**
- Produces `type StudioRouteId`.
- Produces `STUDIO_ROUTES: StudioRoute[]`.
- Produces `findStudioRoute(pathname: string): StudioRoute`.
- Produces `getRoutePath(id: StudioRouteId): string`.

- [ ] **Step 1: Write failing route tests**

Create `apps/studio/src/app/routes.test.ts`:

```ts
import { describe, expect, it } from "vitest";
import { findStudioRoute, getRoutePath, STUDIO_ROUTES } from "./routes";

describe("studio routes", () => {
  it("uses workspace as the authenticated default route", () => {
    expect(getRoutePath("workspace")).toBe("/workspace");
    expect(findStudioRoute("/")).toMatchObject({ id: "workspace", path: "/workspace" });
    expect(findStudioRoute("/unknown")).toMatchObject({ id: "workspace", path: "/workspace" });
  });

  it("contains primary Studio product routes", () => {
    const paths = STUDIO_ROUTES.map((route) => route.path);
    expect(paths).toContain("/create/video");
    expect(paths).toContain("/create/realtime");
    expect(paths).toContain("/assets/avatars");
    expect(paths).toContain("/solutions");
    expect(paths).toContain("/projects");
  });

  it("matches solution kit detail paths with an id parameter", () => {
    expect(findStudioRoute("/solutions/healthcare-guide")).toMatchObject({
      id: "solutionDetail",
      path: "/solutions/:id",
    });
  });
});
```

- [ ] **Step 2: Run tests to verify failure**

Run:

```bash
cd apps/studio
npm test -- src/app/routes.test.ts
```

Expected: FAIL because `./routes` does not exist.

- [ ] **Step 3: Implement routes**

Create `apps/studio/src/app/routes.ts`:

```ts
export type StudioRouteId =
  | "login"
  | "register"
  | "trial"
  | "workspace"
  | "createVideo"
  | "createRealtime"
  | "assets"
  | "assetAvatars"
  | "assetVoices"
  | "assetScenes"
  | "assetBackgrounds"
  | "assetKnowledge"
  | "solutions"
  | "solutionDetail"
  | "projects"
  | "projectDetail"
  | "billing"
  | "team"
  | "apiAccess"
  | "settings";

export type StudioRoute = {
  id: StudioRouteId;
  path: string;
  label: string;
  section: "auth" | "main" | "assets" | "workspace";
};

export const STUDIO_ROUTES: StudioRoute[] = [
  { id: "login", path: "/login", label: "登录", section: "auth" },
  { id: "register", path: "/register", label: "注册", section: "auth" },
  { id: "trial", path: "/trial", label: "免费试用", section: "auth" },
  { id: "workspace", path: "/workspace", label: "首页", section: "main" },
  { id: "createVideo", path: "/create/video", label: "创建视频", section: "main" },
  { id: "createRealtime", path: "/create/realtime", label: "实时数字人", section: "main" },
  { id: "projects", path: "/projects", label: "项目", section: "main" },
  { id: "projectDetail", path: "/projects/:id", label: "项目详情", section: "main" },
  { id: "assets", path: "/assets", label: "资产中心", section: "assets" },
  { id: "assetAvatars", path: "/assets/avatars", label: "数字形象", section: "assets" },
  { id: "assetVoices", path: "/assets/voices", label: "声音资产", section: "assets" },
  { id: "assetScenes", path: "/assets/scenes", label: "场景资产", section: "assets" },
  { id: "assetBackgrounds", path: "/assets/backgrounds", label: "背景资产", section: "assets" },
  { id: "assetKnowledge", path: "/assets/knowledge", label: "知识库", section: "assets" },
  { id: "solutions", path: "/solutions", label: "解决方案包", section: "assets" },
  { id: "solutionDetail", path: "/solutions/:id", label: "方案详情", section: "assets" },
  { id: "billing", path: "/billing", label: "计费", section: "workspace" },
  { id: "team", path: "/team", label: "团队", section: "workspace" },
  { id: "apiAccess", path: "/api-access", label: "API 接入", section: "workspace" },
  { id: "settings", path: "/settings", label: "设置", section: "workspace" },
];

export function getRoutePath(id: StudioRouteId): string {
  return STUDIO_ROUTES.find((route) => route.id === id)?.path ?? "/workspace";
}

function matchParameterizedRoute(pattern: string, pathname: string): boolean {
  if (!pattern.includes(":")) return pattern === pathname;
  const patternParts = pattern.split("/");
  const pathParts = pathname.split("/");
  if (patternParts.length !== pathParts.length) return false;
  return patternParts.every((part, index) => part.startsWith(":") || part === pathParts[index]);
}

export function findStudioRoute(pathname: string): StudioRoute {
  if (pathname === "/") return STUDIO_ROUTES.find((route) => route.id === "workspace")!;
  return (
    STUDIO_ROUTES.find((route) => matchParameterizedRoute(route.path, pathname)) ??
    STUDIO_ROUTES.find((route) => route.id === "workspace")!
  );
}
```

- [ ] **Step 4: Run tests to verify pass**

Run:

```bash
cd apps/studio
npm test -- src/app/routes.test.ts
```

Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add apps/studio/src/app/routes.ts apps/studio/src/app/routes.test.ts
git commit -m "feat: add studio routes"
```

---

### Task 3: Add Product Entities With TDD

**Files:**
- Create: `apps/studio/src/entities/asset.test.ts`
- Create: `apps/studio/src/entities/asset.ts`
- Create: `apps/studio/src/entities/solution-kit.ts`
- Create: `apps/studio/src/entities/project.ts`
- Create: `apps/studio/src/entities/quota.ts`
- Create: `apps/studio/src/entities/user.ts`

**Interfaces:**
- Produces `StudioAsset`, `AssetKind`, `AssetStatus`, `AssetScope`.
- Produces `getReadyAssetsByKind(kind: AssetKind): StudioAsset[]`.
- Produces `getFeaturedAssets(limit?: number): StudioAsset[]`.
- Produces fixture arrays for solution kits and projects.

- [ ] **Step 1: Write failing entity tests**

Create `apps/studio/src/entities/asset.test.ts`:

```ts
import { describe, expect, it } from "vitest";
import { getFeaturedAssets, getReadyAssetsByKind, studioAssets } from "./asset";

describe("studio asset fixtures", () => {
  it("returns only ready assets for a requested kind", () => {
    const avatars = getReadyAssetsByKind("avatar");
    expect(avatars.length).toBeGreaterThan(0);
    expect(avatars.every((asset) => asset.kind === "avatar")).toBe(true);
    expect(avatars.every((asset) => asset.status === "ready")).toBe(true);
  });

  it("orders featured assets by usage count", () => {
    const featured = getFeaturedAssets(3);
    expect(featured).toHaveLength(3);
    expect(featured[0].usageCount).toBeGreaterThanOrEqual(featured[1].usageCount);
    expect(featured[1].usageCount).toBeGreaterThanOrEqual(featured[2].usageCount);
  });

  it("contains solution-ready asset categories", () => {
    const kinds = new Set(studioAssets.map((asset) => asset.kind));
    expect(kinds).toEqual(
      expect.arrayContaining(["avatar", "voice", "background", "scene", "knowledge", "persona", "script_template"]),
    );
  });
});
```

- [ ] **Step 2: Run tests to verify failure**

Run:

```bash
cd apps/studio
npm test -- src/entities/asset.test.ts
```

Expected: FAIL because `./asset` does not exist.

- [ ] **Step 3: Implement entities**

Create `apps/studio/src/entities/asset.ts`, `solution-kit.ts`, `project.ts`, `quota.ts`, and `user.ts` with typed fixtures and helper functions matching the spec.

- [ ] **Step 4: Run tests to verify pass**

Run:

```bash
cd apps/studio
npm test -- src/entities/asset.test.ts
```

Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add apps/studio/src/entities
git commit -m "feat: add studio product entities"
```

---

### Task 4: Build Shared UI And Studio Layout

**Files:**
- Create: `apps/studio/src/shared/design-system/theme.ts`
- Create: `apps/studio/src/shared/ui/Badge.tsx`
- Create: `apps/studio/src/shared/ui/Button.tsx`
- Create: `apps/studio/src/shared/ui/Card.tsx`
- Create: `apps/studio/src/shared/ui/MetricCard.tsx`
- Create: `apps/studio/src/app/StudioLayout.tsx`
- Create: `apps/studio/src/app/AuthLayout.tsx`

**Interfaces:**
- Produces `StudioLayout({ children, activeRouteId })`.
- Produces `AuthLayout({ children, title, subtitle })`.
- Produces reusable UI components for pages.

- [ ] **Step 1: Implement shared design tokens and UI primitives**

Use `studioTheme` tokens from the design spec. `Button` must support `variant="primary" | "secondary" | "ghost"` and use the warm action color only for primary.

- [ ] **Step 2: Implement Studio layout**

Add left sidebar groups for Main, Assets, and Workspace. Add topbar search, quota pill, notification/help buttons, and user account chip.

- [ ] **Step 3: Implement Auth layout**

Add a centered light panel with brand mark, title, subtitle, and children.

- [ ] **Step 4: Verify typecheck**

Run:

```bash
cd apps/studio
npm run typecheck
```

Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add apps/studio/src/shared apps/studio/src/app/StudioLayout.tsx apps/studio/src/app/AuthLayout.tsx
git commit -m "feat: add studio layout system"
```

---

### Task 5: Compose Core Pages And Routing

**Files:**
- Modify: `apps/studio/src/app/App.tsx`
- Create: `apps/studio/src/pages/AuthPages.tsx`
- Create: `apps/studio/src/pages/WorkspacePage.tsx`
- Create: `apps/studio/src/pages/CreateVideoPage.tsx`
- Create: `apps/studio/src/pages/CreateRealtimePage.tsx`
- Create: `apps/studio/src/pages/AssetLibraryPage.tsx`
- Create: `apps/studio/src/pages/SolutionKitsPage.tsx`
- Create: `apps/studio/src/pages/SolutionKitDetailPage.tsx`
- Create: `apps/studio/src/pages/ProjectsPage.tsx`
- Create: `apps/studio/src/pages/UtilityPages.tsx`
- Create: `apps/studio/src/features/assets/AssetCard.tsx`
- Create: `apps/studio/src/features/assets/AssetGrid.tsx`
- Create: `apps/studio/src/features/solution-kits/SolutionKitCard.tsx`
- Create: `apps/studio/src/features/projects/ProjectList.tsx`

**Interfaces:**
- `App` routes by `window.location.pathname` and handles navigation with History API.
- Pages consume fixtures from `entities/`.

- [ ] **Step 1: Create feature cards and lists**

Create asset cards, solution kit cards, and project list components using shared UI primitives.

- [ ] **Step 2: Create Workspace page**

Show primary creation actions, quota summary, recommended assets, recommended solution kits, recent projects, and runtime readiness summary.

- [ ] **Step 3: Create workflow shell pages**

Create video, real-time, asset library, solution kits, solution detail, projects, billing, team, API access, settings, login, register, and trial pages.

- [ ] **Step 4: Replace temporary App with route dispatcher**

Use `findStudioRoute(window.location.pathname)` and `StudioLayout` for authenticated routes. Use `AuthLayout` for login/register/trial.

- [ ] **Step 5: Verify typecheck**

Run:

```bash
cd apps/studio
npm run typecheck
```

Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add apps/studio/src
git commit -m "feat: add studio pages"
```

---

### Task 6: Final Verification And Browser QA

**Files:**
- Modify only if verification reveals layout defects.

**Interfaces:**
- `npm run build` succeeds.
- Studio can be opened locally with Vite.

- [ ] **Step 1: Run full Studio verification**

Run:

```bash
cd apps/studio
npm test
npm run typecheck
npm run build
```

Expected: all commands exit with code 0.

- [ ] **Step 2: Start dev server**

Run:

```bash
cd apps/studio
npm run dev -- --port 5290
```

Expected: Vite serves Studio at `http://127.0.0.1:5290/`.

- [ ] **Step 3: Browser-check responsive layout**

Check `/workspace`, `/assets`, `/solutions`, and `/create/realtime` at widths 375, 768, 1024, and 1440. Verify no horizontal scroll, no text overlap, and visible focus styling.

- [ ] **Step 4: Commit fixes if needed**

```bash
git add apps/studio
git commit -m "fix: polish studio responsive layout"
```

- [ ] **Step 5: Report**

Report final commands, local URL, commits, and any known limitations.

---

## Self-Review

Spec coverage:

- New `apps/studio` app: Task 1.
- TypeScript route structure: Task 2.
- Product asset and solution-kit model: Task 3.
- Light mint visual system and warm CTA: Task 4.
- Workspace, asset center, solution kits, projects, and shell pages: Task 5.
- Build and responsive verification: Task 6.

Placeholder scan:

- The plan contains no TBD/TODO/FIXME placeholders.
- Phase 1 intentionally uses typed fixtures and shell pages; backend integration is assigned to later phases by the design spec.

Type consistency:

- `StudioRouteId`, `StudioAsset`, `SolutionKit`, and `StudioProject` are defined before being consumed.
- Route ids used by layout and pages match the ids defined in Task 2.
